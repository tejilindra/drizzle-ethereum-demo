{
  "name": "drizzle-ethereum-demo",
  "version": "1.0.0",
  "description": "drizzle-ethereum-demo",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ganache-cli": "^6.7.0"
  }
}
